# Hookbook

> Your project's hook reference. If you enable the `hookbook-sync` hook, it
> blocks `Stop` whenever a hook file changes but this file wasn't updated, or
> when the **"N hooks"** count below drifts from the number registered in your
> `settings.json`. That's the point: the guardrails stay documented because the
> gate won't let them rot. Keep the headline count accurate.

This project has **0 hooks** registered.

<!--
For each hook, write one short entry. The format is yours, but a good one
answers: when it fires, what it does, and how to satisfy or override it.
Delete this comment and the example once you add real entries.
-->

## Example entry (delete me)

### `my-hook` · PreToolUse(Bash)
One sentence on what it blocks or warns about and why it exists. **Override:** `MY_HOOK_OVERRIDE=1`.

---

*Starter from Claude Discipline. As you install or write hooks, add an entry here and bump the count. `hookbook-sync` keeps you honest.*
